{
  "name": "Compress",
  "version": "0.1.0",
  "description": "Compress Images",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf dist && NODE_ENV=development BUILD_MAIN=Compress rollup -w -c ../rollup.config.js",
    "build": "rm -rf dist && NODE_ENV=production BUILD_MAIN=Compress rollup -c ../rollup.config.js"
  },
  "author": "Mincle",
  "license": "MIT"
}
